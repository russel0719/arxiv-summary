# arXiv 논문 정리 파이프라인 — 공통 규칙

이 repo는 arXiv cs.CV 신규 논문을 수집해 매일 유용한 논문을 관심사 프로필 기준으로
선별·요약하는 자동화 파이프라인이다.
상세 규칙은 `prompts/daily_rules.md`에 있고, 이 파일에는 headless 실행에 공통으로
적용되는 규칙만 둔다 (`claude -p`가 repo 루트에서 실행되며 이 파일을 자동 로드한다).

## 공통 규칙

- **언어**: 모든 산출물은 한국어로 작성한다. 고유명사·기법·모델명은 영어로 유지한다.
- **git 금지**: `git add`/`commit`/`push` 등 git 명령을 **절대 직접 실행하지 않는다**.
  커밋·푸시는 래퍼 스크립트(`run_digest.sh`)가 처리한다.
- **산출물 범위**: 요청받은 산출물 파일만 생성/수정한다.
- **논문 자체만**: 리포트 본문은 논문을 설명하는 데에만 쓴다. 선별 이유·논문 간 순위·
  실무 적용 판단 등 논문 밖의 이야기는 쓰지 않는다 (상세: `prompts/daily_rules.md`의 '서술 원칙').
  사내 맥락은 별도 프로젝트(`utils/arxiv-internal`)가 이 산출물 위에 덧붙인다.
